<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">

  <!-- 1. Infrastructure (Tables) -->
  <include file="schema/tables.sql" relativeToChangelogFile="true"/>

  <!-- 2. Application Logic (MLE & PL/SQL) -->
  <!-- mle_module.sql is generated by 'npm run build' into the dist folder -->
  <include file="logic/mle_module.sql" relativeToChangelogFile="true"/>
  <include file="logic/plsql_wrappers.sql" relativeToChangelogFile="true"/>

  <!-- 4. API Interface (ORDS) -->
  <include file="api/ords_definitions.sql" relativeToChangelogFile="true"/>

</databaseChangeLog>
